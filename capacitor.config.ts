import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.icematrix.socialportal.go',
  appName: 'Social Portal Go',
  webDir: 'dist'
};

export default config;
